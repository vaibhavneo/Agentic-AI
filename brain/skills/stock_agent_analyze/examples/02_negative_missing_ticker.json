{
  "description": "negative control: missing 'ticker' fails schema validation before dispatch",
  "inputs": {},
  "expected_failure": "INPUT_INVALID",
  "notes": "Never dispatched over HTTP — caught at input_schema.json's required:['ticker']."
}
