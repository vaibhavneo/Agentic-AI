{
 "id": "stock_agent_analyze",
 "name": "Stock Agent Analyze",
 "version": "1.0.0",
 "description": "Dispatch a stock ticker to stock_agent/'s 7-agent financial analysis pipeline (over its running HTTP server) and return the final BUY/SELL/HOLD result.",
 "purpose": "The Central Orchestrator needs one uniform, typed way to hand a ticker to stock_agent/'s existing pipeline. stock_agent/ owns bare top-level packages (agents/, tools/) that collide with brain/'s own internals if imported into the same process, so this skill reaches it over HTTP instead of importing it (D20).",
 "tags": [
  "orchestrator",
  "wrapper",
  "stock_agent",
  "http"
 ],
 "inputs": {
  "schema": "input_schema.json"
 },
 "outputs": {
  "schema": "output_schema.json"
 },
 "memory": {
  "root_param": null,
  "read": [],
  "write": [],
  "append_only": [],
  "immutable": []
 },
 "execution": {
  "contract": "execution_contract.md",
  "steps": [
   "RESOLVE ENDPOINT",
   "CALL OVER HTTP (SSE)",
   "WRAP RESULT"
  ],
  "retries": 0,
  "runtime": {
   "type": "python",
   "entrypoint": "aios_core.runtime.drivers.stock_agent_analyze_driver:run"
  }
 },
 "dependencies": [],
 "evaluation": {
  "contract": "evaluation_contract.md",
  "must": [
   "E1",
   "E2",
   "E3"
  ]
 }
}
